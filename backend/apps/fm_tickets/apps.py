from django.apps import AppConfig


class FmTicketsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.fm_tickets"
    label = "fm_tickets"
    verbose_name = "FM Tickets"

