# Features

Current feature modules include:

- `fm-tickets` for FM Ticketing read, detail, create/edit, comments, assignment, status, and escalation UI
- `maintenance` for maintenance dashboard, work order list, and read-only detail UI
- `master-data` for master-data create/edit forms and supporting frontend workflows
- `dashboard`, `admin`, and `assets` for shared stage-two read and management surfaces
